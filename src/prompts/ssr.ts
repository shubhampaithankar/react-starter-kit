import { Question } from '../utils/types'

const ssr: Question = {
    name: 'ssr',
    type: 'confirm',
    message: 'Use Server Side Rendering? (NextJS)?',
    default: false,
}

export default ssr
