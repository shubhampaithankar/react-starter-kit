import { Question } from '../utils/types'

const hooks: Question = {
    name: 'hooks',
    type: 'list',
    message: 'Select premade hooks:',
    choices: [],
    default: '',
}

export default hooks
